<?xml version="1.0" encoding="UTF-8"?>
<templates xml:space="preserve">

    <t t-name="js_task_toggle.ToggleButtonRES">
        <!-- <button
            class="btn"
            t-att-class="state.value ? 'btn-success' : 'btn-secondary'"
            t-on-click="toggle">

            <t t-esc="state.value ? 'Verified' : 'Not Verified'"/>

        </button>  -->

       <div class="form-check form-switch d-flex align-items-center gap-2">

            <!-- ✅ Field Label -->
            <!-- <span class="fw-bold">
                <t t-esc="props.record.fields[props.name].string"/>
            </span> -->

            <!-- ✅ Properly Bound Toggle -->
            <input class="form-check-input"
                   type="checkbox"
                   role="switch"
                   t-att-checked="state.value"
                   t-on-change="toggle"/>
        </div>


    
    </t>

</templates>
